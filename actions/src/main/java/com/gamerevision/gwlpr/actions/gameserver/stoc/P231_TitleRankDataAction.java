/**
 * For copyright information see the LICENSE document.
 */

/**
 * Auto-generated by PacketCodeGen, on 2012-07-19
 */

package com.gamerevision.gwlpr.actions.gameserver.stoc;

import com.realityshard.shardlet.GenericAction;
import java.nio.BufferOverflowException;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * This is an automatically generated ShardletAction.
 * It resembles the packet template that has been 
 * parsed from our packet templates xml.
 *
 *
 * @author GWLPR Template Updater
 */
public final class P231_TitleRankDataAction extends GenericAction
{

    private int rankId;
    private int data2;
    private int rank;
    private char[] name;


    public short getHeader()
    {
        return 231;
    }


    /**
     * Id of the rank used to refernce it 
     */
    public void setRankId(int newValue)
    {
        rankId = newValue;
    }


    public void setData2(int newValue)
    {
        data2 = newValue;
    }


    /**
     * the rank of the title 
     */
    public void setRank(int newValue)
    {
        rank = newValue;
    }


    /**
     * Name of the rank 
     */
    public void setName(char[] newValue)
    {
        name = newValue;
    }


    private int getSize()
    {
        int size = 16;

        if (name != null)
        {
            size += 2 * name.length;
        }

        return size;
    }


    @Override
    public boolean serialize()
    {
        int size = getSize();

        if (size == 0)
        {
            return false;
        }

        ByteBuffer buffer = ByteBuffer.allocate(size).order(ByteOrder.LITTLE_ENDIAN);

        try
        {
            buffer.putShort(getHeader());

            buffer.putInt(rankId);
            buffer.putInt(data2);
            buffer.putInt(rank);
            short prefix_name;
            if (name == null)
            {
                prefix_name = 0;
            }
            else
            {
                prefix_name = (short) name.length;
            }
            buffer.putShort(prefix_name);
            
            for (int i = 0; i < prefix_name; i++)
            {
                buffer.putChar(name[i]);
            }
        }
        catch (BufferOverflowException e)
        {
            return false;
        }

        setBuffer(buffer);

        return true;
    }
}