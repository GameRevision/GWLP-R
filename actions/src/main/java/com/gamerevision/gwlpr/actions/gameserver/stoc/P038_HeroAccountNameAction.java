/**
 * For copyright information see the LICENSE document.
 */

/**
 * Auto-generated by PacketCodeGen, on 2012-07-19
 */

package com.gamerevision.gwlpr.actions.gameserver.stoc;

import com.realityshard.shardlet.GenericAction;
import java.nio.BufferOverflowException;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * This is an automatically generated ShardletAction.
 * It resembles the packet template that has been 
 * parsed from our packet templates xml.
 *
 *
 * @author GWLPR Template Updater
 */
public final class P038_HeroAccountNameAction extends GenericAction
{

    private char[] accountName;


    public short getHeader()
    {
        return 38;
    }


    public void setAccountName(char[] newValue)
    {
        accountName = newValue;
    }


    private int getSize()
    {
        int size = 4;

        if (accountName != null)
        {
            size += 2 * accountName.length;
        }

        return size;
    }


    @Override
    public boolean serialize()
    {
        int size = getSize();

        if (size == 0)
        {
            return false;
        }

        ByteBuffer buffer = ByteBuffer.allocate(size).order(ByteOrder.LITTLE_ENDIAN);

        try
        {
            buffer.putShort(getHeader());

            short prefix_accountName;
            if (accountName == null)
            {
                prefix_accountName = 0;
            }
            else
            {
                prefix_accountName = (short) accountName.length;
            }
            buffer.putShort(prefix_accountName);
            
            for (int i = 0; i < prefix_accountName; i++)
            {
                buffer.putChar(accountName[i]);
            }
        }
        catch (BufferOverflowException e)
        {
            return false;
        }

        setBuffer(buffer);

        return true;
    }
}