/**
 * For copyright information see the LICENSE document.
 */

/**
 * Auto-generated by PacketCodeGen, on 2012-09-28
 */

package gwlpr.actions.gameserver.stoc;

import com.realityshard.shardlet.utils.GenericAction;
import java.nio.BufferOverflowException;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * This is an automatically generated ShardletAction.
 * It resembles the packet template that has been 
 * parsed from our packet templates xml.
 *
 *
 * @author GWLPR Template Updater
 */
public final class P458_StatusTextAction extends GenericAction
{

    private byte show;
    private byte hasCancel;
    private char[] text;


    public short getHeader()
    {
        return 458;
    }


    public void setShow(byte newValue)
    {
        show = newValue;
    }


    public void sethasCancel(byte newValue)
    {
        hasCancel = newValue;
    }


    public void setText(char[] newValue)
    {
        text = newValue;
    }


    private int getSize()
    {
        int size = 6;

        if (text != null)
        {
            size += 2 * text.length;
        }

        return size;
    }


    @Override
    public boolean serialize()
    {
        int size = getSize();

        if (size == 0)
        {
            return false;
        }

        ByteBuffer buffer = ByteBuffer.allocate(size).order(ByteOrder.LITTLE_ENDIAN);

        try
        {
            buffer.putShort(getHeader());

            buffer.put(show);
            buffer.put(hasCancel);
            short prefix_text;
            if (text == null)
            {
                prefix_text = 0;
            }
            else
            {
                prefix_text = (short) text.length;
            }
            buffer.putShort(prefix_text);
            
            for (int i = 0; i < prefix_text; i++)
            {
                buffer.putChar(text[i]);
            }
        }
        catch (BufferOverflowException e)
        {
            return false;
        }

        setBuffer(buffer);

        return true;
    }
}