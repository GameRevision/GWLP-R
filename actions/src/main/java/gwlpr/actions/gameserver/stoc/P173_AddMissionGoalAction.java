/**
 * For copyright information see the LICENSE document.
 */

/**
 * Auto-generated by PacketCodeGen, on 2012-09-28
 */

package gwlpr.actions.gameserver.stoc;

import realityshard.shardlet.utils.GenericAction;
import java.nio.BufferOverflowException;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * This is an automatically generated ShardletAction.
 * It resembles the packet template that has been 
 * parsed from our packet templates xml.
 *
 *
 * @author GWLPR Template Updater
 */
public final class P173_AddMissionGoalAction extends GenericAction
{

    private char[] goal;
    private int type;


    public short getHeader()
    {
        return 173;
    }


    public void setGoal(char[] newValue)
    {
        goal = newValue;
    }


    public void setType(int newValue)
    {
        type = newValue;
    }


    private int getSize()
    {
        int size = 8;

        if (goal != null)
        {
            size += 2 * goal.length;
        }

        return size;
    }


    @Override
    public boolean serialize()
    {
        int size = getSize();

        if (size == 0)
        {
            return false;
        }

        ByteBuffer buffer = ByteBuffer.allocate(size).order(ByteOrder.LITTLE_ENDIAN);

        try
        {
            buffer.putShort(getHeader());

            short prefix_goal;
            if (goal == null)
            {
                prefix_goal = 0;
            }
            else
            {
                prefix_goal = (short) goal.length;
            }
            buffer.putShort(prefix_goal);
            
            for (int i = 0; i < prefix_goal; i++)
            {
                buffer.putChar(goal[i]);
            }
            buffer.putInt(type);
        }
        catch (BufferOverflowException e)
        {
            return false;
        }

        setBuffer(buffer);

        return true;
    }
}