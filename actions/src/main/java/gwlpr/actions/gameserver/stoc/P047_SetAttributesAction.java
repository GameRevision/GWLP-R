/**
 * For copyright information see the LICENSE document.
 */

/**
 * Auto-generated by PacketCodeGen, on 2012-09-28
 */

package gwlpr.actions.gameserver.stoc;

import realityshard.shardlet.utils.GenericAction;
import java.nio.BufferOverflowException;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * This is an automatically generated ShardletAction.
 * It resembles the packet template that has been 
 * parsed from our packet templates xml.
 *
 *
 * @author GWLPR Template Updater
 */
public final class P047_SetAttributesAction extends GenericAction
{

    private int agentID;
    private int[] data;


    public short getHeader()
    {
        return 47;
    }


    public void setAgentID(int newValue)
    {
        agentID = newValue;
    }


    /**
     * attribute id, normal value, modified value 
     */
    public void setData(int[] newValue)
    {
        data = newValue;
    }


    private int getSize()
    {
        int size = 8;

        if (data != null)
        {
            size += 4 * data.length;
        }

        return size;
    }


    @Override
    public boolean serialize()
    {
        int size = getSize();

        if (size == 0)
        {
            return false;
        }

        ByteBuffer buffer = ByteBuffer.allocate(size).order(ByteOrder.LITTLE_ENDIAN);

        try
        {
            buffer.putShort(getHeader());

            buffer.putInt(agentID);
            short prefix_data;
            if (data == null)
            {
                prefix_data = 0;
            }
            else
            {
                prefix_data = (short) data.length;
            }
            buffer.putShort(prefix_data);
            
            for (int i = 0; i < prefix_data; i++)
            {
                buffer.putInt(data[i]);
            }
        }
        catch (BufferOverflowException e)
        {
            return false;
        }

        setBuffer(buffer);

        return true;
    }
}