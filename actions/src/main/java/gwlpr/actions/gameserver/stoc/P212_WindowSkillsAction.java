/**
 * For copyright information see the LICENSE document.
 */

/**
 * Auto-generated by PacketCodeGen, on 2012-09-28
 */

package gwlpr.actions.gameserver.stoc;

import realityshard.shardlet.utils.GenericAction;
import java.nio.BufferOverflowException;
import java.nio.ByteBuffer;
import java.nio.ByteOrder;

/**
 * This is an automatically generated ShardletAction.
 * It resembles the packet template that has been 
 * parsed from our packet templates xml.
 *
 * This can be used to add skills to a window, such as 
 * at a skill trainer. 
 *
 * @author GWLPR Template Updater
 */
public final class P212_WindowSkillsAction extends GenericAction
{

    private byte unlocked;
    private int[] skillIDs;


    public short getHeader()
    {
        return 212;
    }


    public void setUnlocked(byte newValue)
    {
        unlocked = newValue;
    }


    public void setSkillIDs(int[] newValue)
    {
        skillIDs = newValue;
    }


    private int getSize()
    {
        int size = 5;

        if (skillIDs != null)
        {
            size += 4 * skillIDs.length;
        }

        return size;
    }


    @Override
    public boolean serialize()
    {
        int size = getSize();

        if (size == 0)
        {
            return false;
        }

        ByteBuffer buffer = ByteBuffer.allocate(size).order(ByteOrder.LITTLE_ENDIAN);

        try
        {
            buffer.putShort(getHeader());

            buffer.put(unlocked);
            short prefix_skillIDs;
            if (skillIDs == null)
            {
                prefix_skillIDs = 0;
            }
            else
            {
                prefix_skillIDs = (short) skillIDs.length;
            }
            buffer.putShort(prefix_skillIDs);
            
            for (int i = 0; i < prefix_skillIDs; i++)
            {
                buffer.putInt(skillIDs[i]);
            }
        }
        catch (BufferOverflowException e)
        {
            return false;
        }

        setBuffer(buffer);

        return true;
    }
}